<div class="modal fade" id="transactionLogsHelp" tabindex="-1" aria-labelledby="transactionLogsHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="transactionLogsHelpLabel">Transaction Logs Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>Transaction logs track insert/update/delete operations with before/after snapshots.</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Filter by entity name, operation, and date range.</li>
                        <li>Sort by time to review the latest changes.</li>
                        <li>Open Details to inspect snapshots.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review before/after JSON and changed fields.</li>
                        <li>Use correlationId to link to request logs.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Auto-refresh</h6>
                    <ul class="mb-0">
                        <li>Enable Auto-refresh to receive new transactions via SignalR.</li>
                        <li>Use the SignalR status badge to confirm connectivity.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>No data:</strong> ensure audit logging is enabled.</li>
                        <li><strong>SignalR disconnected:</strong> reload the page and check API base URL.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

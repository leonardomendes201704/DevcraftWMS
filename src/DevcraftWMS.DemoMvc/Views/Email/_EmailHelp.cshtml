<div class="modal fade" id="emailHelp" tabindex="-1" aria-labelledby="emailHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="emailHelpLabel">Email Test Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>This screen lets you send or preview emails through the configured email pipeline.</p>
                </section>

                <section class="mb-4">
                    <h6>Send</h6>
                    <ul class="mb-0">
                        <li>Fill To, Subject, and Body.</li>
                        <li>Toggle HTML Body if the content is HTML.</li>
                        <li>Click <strong>Send</strong> to trigger the email flow.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Preview</h6>
                    <ul class="mb-0">
                        <li>Click <strong>Preview</strong> to render content without sending.</li>
                        <li>Use the Preview panel to validate formatting.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Recent Sends</h6>
                    <ul class="mb-0">
                        <li>Shows the latest send attempts and statuses.</li>
                        <li>Use it to verify pipeline health.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>No recent sends:</strong> send a test email to create logs.</li>
                        <li><strong>Validation errors:</strong> check required fields.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

@model DevcraftWMS.DemoMvc.ViewModels.Locations.LocationFormViewModel
@{
    ViewData["Title"] = "New Location";
    ViewData["Subtitle"] = "Create a new bin/location";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <partial name="_LocationForm" model="Model" />
            <div class="mt-4 d-flex gap-2">
                <button type="submit" class="btn btn-primary">Save</button>
                <a class="btn btn-light border" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

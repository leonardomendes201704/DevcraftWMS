<div class="modal fade" id="sectionsHelp" tabindex="-1" aria-labelledby="sectionsHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="sectionsHelpLabel">Sections Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>Sections subdivide sectors into smaller operational areas for storage and picking.</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Select a warehouse and sector to scope the list.</li>
                        <li>Filter by code and name, and control activity status.</li>
                        <li>Use sorting and pagination for navigation.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Create</h6>
                    <ul class="mb-0">
                        <li>Click <strong>New Section</strong> to add a section under a sector.</li>
                        <li>Required: Code and Name.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review section metadata and status.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Edit</h6>
                    <ul class="mb-0">
                        <li>Update section details and save changes.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Deactivate</h6>
                    <ul class="mb-0">
                        <li>Deactivate a section to remove it from active operations.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>No sector:</strong> create a sector first.</li>
                        <li><strong>Duplicate code:</strong> section code must be unique per sector.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

@model DevcraftWMS.DemoMvc.ViewModels.Sectors.SectorFormViewModel
@{
    ViewData["Title"] = "New Sector";
    ViewData["Subtitle"] = "Create a new warehouse sector";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <partial name="_SectorForm" model="Model" />
            <div class="mt-4 d-flex gap-2">
                <button type="submit" class="btn btn-primary">Save</button>
                <a class="btn btn-light border" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

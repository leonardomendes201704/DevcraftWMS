@model DevcraftWMS.DemoMvc.ViewModels.Uoms.UomFormViewModel
@{
    ViewData["Title"] = "New UoM";
    ViewData["Subtitle"] = "Create a unit of measure";
}

<div class="card">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <partial name="_UomForm" model="Model" />
            <div class="mt-4 d-flex gap-2">
                <button type="submit" class="btn btn-primary">Save</button>
                <a class="btn btn-light border" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

<div class="modal fade" id="uomsHelp" tabindex="-1" aria-labelledby="uomsHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="uomsHelpLabel">Units of Measure Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>UoM defines measurement units used by products and conversions (e.g., unit, box, pallet).</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Filter by code, name, or type.</li>
                        <li>Use Status and Include Inactive for visibility control.</li>
                        <li>Sort columns by clicking headers.</li>
                        <li>Use pagination for large catalogs.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Create</h6>
                    <ul class="mb-0">
                        <li>Click <strong>New UoM</strong> to add a unit.</li>
                        <li>Required: Code, Name, Type.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Edit</h6>
                    <ul class="mb-0">
                        <li>Update unit details and save changes.</li>
                        <li>Be consistent with units referenced by products.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review unit details and status.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Deactivate</h6>
                    <ul class="mb-0">
                        <li>Deactivate units that should no longer be used.</li>
                        <li>Inactive units remain available in history and reports.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>Duplicate code:</strong> UoM code must be unique.</li>
                        <li><strong>Type mismatch:</strong> ensure type fits product usage.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="warehousesHelp" tabindex="-1" aria-labelledby="warehousesHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="warehousesHelpLabel">Warehouses Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>Warehouses define physical facilities and operational capabilities (receiving, picking, shipping).</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Search by code or name and filter by location fields.</li>
                        <li>Use filters for type, primary flag, and activity status.</li>
                        <li>Sort columns and control pagination.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Create</h6>
                    <ul class="mb-0">
                        <li>Click <strong>New Warehouse</strong> to register a site.</li>
                        <li>Fill address, contact, and capacity details.</li>
                        <li>Configure operational flags (receiving, picking, shipping, returns).</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review address, contact, and capacity summary.</li>
                        <li>Use this view as the starting point for dependent structures.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Edit</h6>
                    <ul class="mb-0">
                        <li>Update operational settings and metadata.</li>
                        <li>Ensure code remains unique per customer.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Deactivate</h6>
                    <ul class="mb-0">
                        <li>Deactivate to remove from active operations.</li>
                        <li>Inactive warehouses can still be queried if included.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>Duplicate code:</strong> warehouse code must be unique.</li>
                        <li><strong>Missing address:</strong> ensure required address fields are filled.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

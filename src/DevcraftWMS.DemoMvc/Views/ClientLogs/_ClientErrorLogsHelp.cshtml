<div class="modal fade" id="clientErrorLogsHelp" tabindex="-1" aria-labelledby="clientErrorLogsHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="clientErrorLogsHelpLabel">Frontend Errors Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>Frontend Errors capture Demo MVC client telemetry (UI errors and API call failures).</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Filter by event type, severity, and date range.</li>
                        <li>Use Details to inspect client context and API call metadata.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review client URL, route, and API call status when available.</li>
                        <li>Use correlationId to cross-reference server logs.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Auto-refresh</h6>
                    <ul class="mb-0">
                        <li>Enable Auto-refresh to receive new client events via SignalR.</li>
                        <li>Use the SignalR status badge to confirm connectivity.</li>
                    </ul>
                </section>

                <section>
                    <h6>Common Issues</h6>
                    <ul class="mb-0">
                        <li><strong>No events:</strong> ensure telemetry is enabled in DemoMvc and API.</li>
                        <li><strong>SignalR disconnected:</strong> reload the page and confirm API base URL.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="zonesHelp" tabindex="-1" aria-labelledby="zonesHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="zonesHelpLabel">Zones Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>Zones define functional areas in a warehouse such as staging, picking, bulk, quarantine, and cross-dock.</p>
                </section>

                <section class="mb-4">
                    <h6>Index (List)</h6>
                    <ul class="mb-0">
                        <li>Select a warehouse to list its zones.</li>
                        <li>Filter by code, name, type, and status.</li>
                        <li>Sort columns and use pagination for large lists.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Create</h6>
                    <ul class="mb-0">
                        <li>Click <strong>New Zone</strong> to register a zone.</li>
                        <li>Required fields: Warehouse, Code, Name, Type.</li>
                        <li>Use Description for operational notes.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Details</h6>
                    <ul class="mb-0">
                        <li>Review zone attributes and status.</li>
                        <li>Zones can be assigned to locations when registering bins.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Edit</h6>
                    <ul class="mb-0">
                        <li>Update code, name, type, or description.</li>
                        <li>Code must remain unique per warehouse.</li>
                    </ul>
                </section>

                <section>
                    <h6>Deactivate</h6>
                    <ul class="mb-0">
                        <li>Deactivate zones that are no longer used.</li>
                        <li>Inactive zones are hidden unless filtered.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

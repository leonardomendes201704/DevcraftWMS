<div class="modal fade" id="portariaDashboardHelp" tabindex="-1" aria-labelledby="portariaDashboardHelpLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="portariaDashboardHelpLabel">Portaria Dashboard Help</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <section class="mb-4">
                    <h6>Purpose</h6>
                    <p>This dashboard gives a quick overview of inbound activity handled by the gatehouse team.</p>
                </section>

                <section class="mb-4">
                    <h6>API Status</h6>
                    <ul class="mb-0">
                        <li><strong>Online</strong> confirms the API is reachable.</li>
                        <li><strong>Offline</strong> indicates API connectivity issues; retry after checking the API.</li>
                    </ul>
                </section>

                <section class="mb-4">
                    <h6>Upcoming Modules</h6>
                    <ul class="mb-0">
                        <li><strong>ASNs:</strong> register inbound shipments before arrival.</li>
                        <li><strong>Inbound Orders:</strong> schedule dock arrivals and check-in.</li>
                        <li><strong>Receipts:</strong> confirm unloading and handoff to receiving.</li>
                    </ul>
                </section>

                <section>
                    <h6>Next Steps</h6>
                    <ul class="mb-0">
                        <li>Use the customer selector in the top bar to scope the portal data.</li>
                        <li>Log in with the same credentials used in the WMS API.</li>
                    </ul>
                </section>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light border" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>
